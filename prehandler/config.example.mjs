import path from 'path';

export const config = {
    imgRootDir: path.resolve('C:/user/imgdir'),
    serverImgBaseURL: "http://localhost",
    metaFilename: 'meta.json',
    resourceJsonOutputPath: path.resolve('./src/assets/resource.json'),
};
